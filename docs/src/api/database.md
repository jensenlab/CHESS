# CHESSDatabase API Reference

The append-only ledger and reconstruction layer -- persists `CHESSCore` objects to SQLite and
replays the ledger to reconstruct them.

```@autodocs
Modules = [CHESS.CHESSDatabase]
```
