# CHESSParsers

Parses lab-instrument export files into `DataFrame`/[`LabwareRead`](@ref)/[`EnvironmentLog`](@ref)/
JSON. Not re-exported by `CHESS` -- use directly via `using CHESSParsers`.

```@autodocs
Modules = [CHESSParsers]
```
