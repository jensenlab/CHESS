# CHESSCore API Reference

The "lab engine" -- `Location`/`Stock`/`Attribute`/`Read` types and the pure, in-memory operations
that act on them. No database dependency.

```@autodocs
Modules = [CHESS.CHESSCore]
```
