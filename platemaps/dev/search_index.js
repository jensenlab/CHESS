var documenterSearchIndex = {"docs":
[{"category":"section","location":"quickstart/#Quick-Start-Guide","page":"Quick Start Guide","text":"","title":"Quick Start Guide"},{"category":"section","location":"quickstart/#Standalone-scheduling","page":"Quick Start Guide","text":"Build edges with mkedge – (node1, node2, role, metadata) – and hand them to schedule_platemap along with the nodes you want fixed first (typically \"runs\") and an active-well mask:\n\nusing PlateMaps\n\nwells = trues(4, 4)\nrun_nodes = [Symbol(\"run$i\") for i in 1:4]\nedges = [mkedge(Symbol(\"run$i\"), :pos1, :positive) for i in 1:4]\nappend!(edges, [mkedge(Symbol(\"run$i\"), :neg1, :negative) for i in 1:4])\n\npms = schedule_platemap(wells, edges, run_nodes)\n\nschedule_platemap always returns a Vector{PlateMap} – one entry per plate actually used. Edge-connected components are atomic (never split across plates): if everything fits on one plate, you get a 1-element vector back.\n\npm = only(pms)\nwell_position(pm, :run1)   # CartesianIndex of run1's well\nnodes(pm)                  # every placed node\n\nsolver=\"exchange\" (default, heuristic) or solver=\"MILP\" (exact, objective=:distance only) picks the control-placement algorithm; plate_solver=\"greedy\" (default) or \"MILP\" picks how components are binned onto plates when more than one is needed.","title":"Standalone scheduling"},{"category":"section","location":"quickstart/#With-RunMaps","page":"Quick Start Guide","text":"RunMaps.RunMap models run/control relationships as a graph with open-ended relation_type edges – it has no structural \"run\" vs. \"control\" distinction, so you tell schedule_platemap which relation types mark a node as placeable (optimized) rather than fixed:\n\nusing PlateMaps, RunMaps\n\nrm = RunMap{Symbol}()\nfor i in 1:4\n    link!(rm, Symbol(\"run$i\"), :pos1, :positive)\n    link!(rm, Symbol(\"run$i\"), :neg1, :negative)\nend\n\npms = schedule_platemap(wells, rm, (:positive, :negative))\npm = only(pms)\n\ndescribe(pm, rm, :run1)   # (registered=true, outgoing_roles=[...], incoming_roles=[...], ...)\nplot(pm, rm)              # role-colored layout\nDataFrame(pm, rm)         # joined well + role/metadata view\n\nAny node that participates, as either endpoint, in an edge whose relation_type is in placeable_roles is placed by the control-placement stage; every other node is fixed first via place_runs.","title":"With RunMaps"},{"category":"section","location":"quickstart/#With-CHESSCore","page":"Quick Start Guide","text":"A registered plate LocationKind can stand in for a hand-built wells::BitMatrix:\n\nusing PlateMaps, CHESSCore\n\nkind = CHESSCore.LocationKind(:MyPlate; shape=(8, 12))\npms = schedule_platemap(kind, edges, run_nodes)\n\nThis composes with the RunMaps extension automatically when both are loaded – schedule_platemap(kind, rm, placeable_roles; kwargs...) works with no extra glue code.","title":"With CHESSCore"},{"category":"section","location":"quickstart/#DataFrame-and-JSON-interfaces","page":"Quick Start Guide","text":"A single plate:\n\ndf = DataFrame(pm)\nPlateMap(df) == pm\n\njson_to_platemap(platemap_to_json(pm)) == pm\n\nA multi-plate batch compiles into one file for each interface, disambiguated by a leading plate column (DataFrame) or a \"PlateMapBatch\"-tagged wrapper (JSON):\n\ndf = DataFrame(pms)                    # one \"plate\" column, stacked rows\nplatemaps_from_dataframe(df) == pms\n\njson_to_platemaps(platemaps_to_json(pms)) == pms\n\nThe RunMaps-joined form works the same way: DataFrame(pms::Vector{PlateMap}, rm::RunMap).","title":"DataFrame and JSON interfaces"},{"category":"section","location":"api-reference/#API-Reference","page":"API Reference","text":"\n\n","title":"API Reference"},{"category":"section","location":"api-reference/#Full-docstrings","page":"API Reference","text":"","title":"Full docstrings"},{"category":"method","location":"api-reference/#DataFrames.DataFrame-Tuple{Vector{PlateMap}}","page":"API Reference","text":"DataFrame(pms::Vector{PlateMap}) -> DataFrame\n\nCompile a multi-plate schedule into a single DataFrame: DataFrame(pm) for each plate, stacked with a leading plate column (1-based, matching pms' order) to disambiguate rows across plates (well \"A1\" exists on every plate). See platemaps_from_dataframe for the reverse.\n\n\n\n\n\n","title":"DataFrames.DataFrame"},{"category":"type","location":"api-reference/#PlateMaps.PlacementError","page":"API Reference","text":"struct PlacementError <: Exception\n\nThrown when a PlateMap's occupant field is invalid: a node assigned to an inactive well, or the same node assigned to more than one well.\n\n\n\n\n\n","title":"PlateMaps.PlacementError"},{"category":"type","location":"api-reference/#PlateMaps.PlateMap","page":"API Reference","text":"struct PlateMap{T}\n    wells::BitMatrix\n    occupant::Matrix{Union{Missing,T}}\nend\n\nA PlateMap is the physical realization of a scheduling problem: which node (of caller-chosen identity type T) occupies which well of a plate. It stores nothing about why – no roles, no run/control distinction, no relationships between nodes – only where. PlateMap is deliberately the solution, not the problem statement; combine it with whatever produced the schedule (e.g. a RunMaps.RunMap, sharing the same node identities) to resolve what any given occupant actually is.\n\noccupant is missing at every unassigned well (including every inactive one).\n\n\n\n\n\n","title":"PlateMaps.PlateMap"},{"category":"method","location":"api-reference/#PlateMaps.PlateMap-Union{Tuple{AbstractMatrix}, Tuple{T}} where T","page":"API Reference","text":"PlateMap{T}(wells::AbstractMatrix) where T\n\nConstruct an empty PlateMap (no nodes placed) over the given active-well mask.\n\n\n\n\n\n","title":"PlateMaps.PlateMap"},{"category":"constant","location":"api-reference/#PlateMaps.NODE_TYPE_REGISTRY","page":"API Reference","text":"NODE_TYPE_REGISTRY\n\nMaps a serialized type-tag string to the concrete Julia type it should reconstruct as when parsing a PlateMap's JSON occupant field back in – JSON has no native way to represent arbitrary Julia types (a Symbol-typed occupant, for instance, would otherwise silently come back as a String). Mirrors Pourfecto's INSTRUMENT_TYPES tag-to-type registry pattern for the same \"which concrete type do I rebuild\" problem. Extend it (NODE_TYPE_REGISTRY[\"MyType\"] = MyType) for custom node-identity types.\n\n\n\n\n\n","title":"PlateMaps.NODE_TYPE_REGISTRY"},{"category":"constant","location":"api-reference/#PlateMaps.plate_solvers","page":"API Reference","text":"plate_solvers\n\nMaps a plate_solver string to the function that implements it: \"greedy\" => _plate_assign_greedy, \"MILP\" => _plate_assign_MILP. Selected via schedule_platemap's plate_solver keyword.\n\n\n\n\n\n","title":"PlateMaps.plate_solvers"},{"category":"method","location":"api-reference/#Base.getindex-Tuple{PlateMap, Any, Any}","page":"API Reference","text":"Base.getindex(pm::PlateMap, i, j)\nBase.getindex(pm::PlateMap, I::CartesianIndex)\n\nReturn the node occupying well (i,j), or missing if that well is empty/inactive. Reserved strictly for well-based access – for the reverse lookup (given a node, which well is it in), see well_position; pm[node] is deliberately not defined, since if the node identity type T is e.g. Int, pm[5] would be ambiguous between \"well 5\" and \"node 5\".\n\n\n\n\n\n","title":"Base.getindex"},{"category":"function","location":"api-reference/#Base.iterate","page":"API Reference","text":"Base.iterate(pm::PlateMap)\n\nIterate over pm's occupied wells as node => well pairs (CartesianIndex), skipping empty/inactive wells – mirrors ControlMap's own philosophy of iterating over the meaningful relationships rather than the raw underlying matrix.\n\n\n\n\n\n","title":"Base.iterate"},{"category":"method","location":"api-reference/#PlateMaps._component_groups-Tuple{Any, Any}","page":"API Reference","text":"_component_groups(edges, fixed_nodes) -> Vector{Set}\n\nThe atomic node-groups a multi-plate schedule must never split: every edge-connected component of edges (see components), plus a singleton Set for any fixed_nodes entry not already covered by one (a fixed node with no edges at all is still its own atomic unit of size 1).\n\n\n\n\n\n","title":"PlateMaps._component_groups"},{"category":"method","location":"api-reference/#PlateMaps._exchange_solve-Tuple{BitMatrix, Any, AbstractDict}","page":"API Reference","text":"_exchange_solve(wells, edges, fixed_pos; objective=:hybrid, minimize=true, restarts=10, iterations=1000,\n                 lambda=0.5, rng=Random.default_rng(), kwargs...) -> Dict{Any,CartesianIndex{2}}\n\nHeuristic local-search solver: place every node referenced by edges that isn't already in fixed_pos (i.e. every control node, with run nodes already fixed by place_runs) onto wells's remaining active wells, minimizing (or maximizing) the chosen objective. Returns the full position dict (fixed_pos merged with the placed controls) – the internal counterpart MILP's _milp_solve mirrors; schedule_platemap is the public entry point that calls one of these via solver=. Accepts and ignores extra kwargs (e.g. optimizer/timelimit, meant for plate_solver=\"MILP\"), since schedule_platemap forwards one shared keyword bag to both the plate solver and this control solver.\n\n\n\n\n\n","title":"PlateMaps._exchange_solve"},{"category":"method","location":"api-reference/#PlateMaps._local_search-Tuple{BitMatrix, Vector, Vector{CartesianIndex{2}}, AbstractDict, Function, Bool, Int64, Int64, Random.AbstractRNG}","page":"API Reference","text":"_local_search(wells, nodes, free_wells, fixed_pos, score, minimize, restarts, iterations, rng)\n    -> (Dict{Any,CartesianIndex{2}}, Real)\n\nGeneric restart/local-move search: place nodes onto free_wells, minimizing (or maximizing) score (called on merge(fixed_pos, candidate)). Each restart starts from a random placement and repeatedly either relocates one node into a currently-empty free well or swaps two nodes' positions, keeping a move only if it improves the score. Used both for control placement (relative to already-fixed runs) and for run placement itself (relative to nothing, i.e. fixed_pos=Dict(), minimizing run_compactness).\n\n\n\n\n\n","title":"PlateMaps._local_search"},{"category":"method","location":"api-reference/#PlateMaps._milp_solve-Tuple{BitMatrix, Any, AbstractDict}","page":"API Reference","text":"_milp_solve(wells, edges, fixed_pos; objective=:distance, optimizer=_default_optimizer[], timelimit=100,\n            rng=Random.default_rng()) -> Dict{Any,CartesianIndex{2}}\n\nExact solver for control placement relative to already-fixed run positions, generalizing PlateArrays' old ring-trick MILP from two hardcoded roles to however many are present. This is the two-stage design that reopens MILP tractability after an earlier, fully-joint formulation was found intractable (see the design notes): with run positions fixed before the solve, a run's own nearest-same-role-control term is exactly the old ring-trick (a fixed query point, an unknown-position target), and a control's own nearest-same-role-run term is even simpler than before – every run it could link to sits at a known, fixed position, so \"distance to the nearest one\" is a constant, precomputable per candidate well, not a ring at all.\n\nRequires a bipartite edge set: every control's same-role neighbors must themselves be fixed nodes (never another control) – true whenever edges comes from a ControlMap (or anything shaped like one), since its edges only ever connect a run to a control. Throws ArgumentError if this doesn't hold.\n\nOnly objective=:distance is supported – :evenness/:hybrid aren't yet formulated for the exact solver (they'd need the per-role row/column margin terms linearized too); use solver=\"exchange\" for those.\n\nAccepts and ignores extra kwargs (e.g. run_restarts/run_iterations, meant for place_runs), since schedule_platemap forwards one shared keyword bag to both the plate solver and this control solver.\n\n\n\n\n\n","title":"PlateMaps._milp_solve"},{"category":"method","location":"api-reference/#PlateMaps._neighboring_disk-NTuple{5, Int64}","page":"API Reference","text":"_neighboring_disk(row,col,ring,R,C) -> Vector{Tuple{Int,Int}}\n\nEvery grid cell reachable within ring 4-connected hops of (row,col) (a cumulative disk, not a shell) – ported from PlateArrays' old neighboring_ring (the ring-trick's building block).\n\n\n\n\n\n","title":"PlateMaps._neighboring_disk"},{"category":"method","location":"api-reference/#PlateMaps._plate_assign_MILP-Tuple{Vector{Int64}, Int64}","page":"API Reference","text":"_plate_assign_MILP(weights::Vector{Int}, capacity::Int; optimizer=_default_optimizer[], timelimit=100) -> Vector{Int}\n\nMILP formulation of the same weighted plate-assignment problem as _plate_assign_greedy: partition items into the minimum number of plates whose total weight never exceeds capacity. Exact (given enough time) for general/mixed weights, unlike the greedy heuristic. The candidate plate count G is sized from running greedy first as a valid upper bound. Throws ArgumentError if any weight exceeds capacity. Accepts and ignores extra kwargs (e.g. the control solver's objective/restarts/...), same as _plate_assign_greedy, since schedule_platemap forwards one shared keyword bag to both the plate solver and the control solver.\n\n\n\n\n\n","title":"PlateMaps._plate_assign_MILP"},{"category":"method","location":"api-reference/#PlateMaps._plate_assign_greedy-Tuple{Vector{Int64}, Int64}","page":"API Reference","text":"_plate_assign_greedy(weights::Vector{Int}, capacity::Int; kwargs...) -> Vector{Int}\n\nPartition items (given by weights, e.g. atomic-component sizes) into groups (plates) whose total weight never exceeds capacity, using first-fit-decreasing (FFD): sort items by weight descending, place each into the first group with enough remaining capacity, opening a new group otherwise. Exact optimal group count for uniform weights; a good heuristic (not guaranteed exact) for mixed weights – see _plate_assign_MILP for an exact alternative. Throws ArgumentError if any weight exceeds capacity (a component that can never fit on any plate). Accepts and ignores extra kwargs so it is interchangeable with _plate_assign_MILP behind plate_solvers.\n\n\n\n\n\n","title":"PlateMaps._plate_assign_greedy"},{"category":"method","location":"api-reference/#PlateMaps._schedule_single_plate-Tuple{BitMatrix, Any, Any}","page":"API Reference","text":"_schedule_single_plate(wells, edges, fixed_nodes; solver, run_restarts, run_iterations, rng, kwargs...)\n    -> PlateMap\n\nTwo-stage single-plate scheduler: fix fixed_nodes' positions first (via place_runs, clustering edge-connected groups of them together), then place every other node referenced by edges around them, minimizing (or maximizing) the chosen objective. schedule_platemap is the public entry point; this is the per-plate primitive it calls once per plate a multi-plate schedule needs.\n\nThis two-stage shape is what makes an exact solver viable: PlateArrays' old MILP solver was tractable specifically because run positions were fixed before it ran; an earlier, fully-joint design of this package (every node placed simultaneously) made exact solving intractable even at modest scale.\n\n\n\n\n\n","title":"PlateMaps._schedule_single_plate"},{"category":"method","location":"api-reference/#PlateMaps.components-Tuple{Any}","page":"API Reference","text":"components(edges) -> Vector{Set}\n\nThe connected components of the graph formed by edges (both endpoints of every edge, regardless of role) – a plain union-find over node identities. Nodes that never appear in edges are not included.\n\n\n\n\n\n","title":"PlateMaps.components"},{"category":"function","location":"api-reference/#PlateMaps.describe","page":"API Reference","text":"describe(pm::PlateMap, cm, node)\n\nResolve what node (or the occupant at a given well) actually is, using whatever produced the schedule – e.g. a RunMaps.RunMap (what relation_types touch it? what metadata?). PlateMap itself stores none of this; it's provided by the RunMaps package extension (using RunMaps activates it) rather than the core, since it needs both structures at once.\n\n\n\n\n\n","title":"PlateMaps.describe"},{"category":"method","location":"api-reference/#PlateMaps.edge_nodes-Tuple{Any}","page":"API Reference","text":"edge_nodes(edges) -> Set\n\nThe distinct nodes referenced by edges (either endpoint).\n\n\n\n\n\n","title":"PlateMaps.edge_nodes"},{"category":"method","location":"api-reference/#PlateMaps.evenness-Tuple{BitMatrix, Any, Symbol, Any}","page":"API Reference","text":"evenness(wells::BitMatrix,pos,role::Symbol,edges) -> Real\n\nSum of squared deviations between one role's actual per-row/per-column well counts and the count expected if that role's nodes were spread uniformly across the plate's active wells – generalizes PlateArrays' old LHS metric from two hardcoded roles to whichever roles are actually present.\n\n\n\n\n\n","title":"PlateMaps.evenness"},{"category":"method","location":"api-reference/#PlateMaps.expected_margins-Tuple{BitMatrix, Integer}","page":"API Reference","text":"expected_margins(wells::BitMatrix,count::Integer) -> (rowexp,colexp)\n\nExpected per-row/per-column count if count items were spread uniformly across wells' active cells.\n\n\n\n\n\n","title":"PlateMaps.expected_margins"},{"category":"method","location":"api-reference/#PlateMaps.hybrid-Tuple{BitMatrix, Any, Any}","page":"API Reference","text":"hybrid(wells,pos,edges;lambda=0.5,lb_dist=0,ub_dist=1,lb_even=0,ub_even=1) -> Real\n\nWeighted combination of the (normalized) distance and evenness objectives – generalizes PlateArrays' hybrid, now over however many roles/edges are actually present rather than two hardcoded ones.\n\n\n\n\n\n","title":"PlateMaps.hybrid"},{"category":"method","location":"api-reference/#PlateMaps.json_to_platemap-Tuple{AbstractString}","page":"API Reference","text":"json_to_platemap(s::AbstractString) -> PlateMap\n\nParse a JSON string (produced by platemap_to_json) back into a PlateMap. The node-identity type is reconstructed via NODE_TYPE_REGISTRY.\n\n\n\n\n\n","title":"PlateMaps.json_to_platemap"},{"category":"method","location":"api-reference/#PlateMaps.json_to_platemaps-Tuple{AbstractString}","page":"API Reference","text":"json_to_platemaps(s::AbstractString) -> Vector{PlateMap}\n\nParse a JSON string (produced by platemaps_to_json) back into a Vector{PlateMap}.\n\n\n\n\n\n","title":"PlateMaps.json_to_platemaps"},{"category":"method","location":"api-reference/#PlateMaps.manhattan_distance-Tuple{CartesianIndex, CartesianIndex}","page":"API Reference","text":"manhattan_distance(p1::CartesianIndex,p2::CartesianIndex) -> Int\n\nL1 distance between two well positions.\n\n\n\n\n\n","title":"PlateMaps.manhattan_distance"},{"category":"method","location":"api-reference/#PlateMaps.margins-Tuple{BitMatrix}","page":"API Reference","text":"margins(wells::BitMatrix) -> (rowsums, colsums)\n\nActive-well counts per row and per column.\n\n\n\n\n\n","title":"PlateMaps.margins"},{"category":"function","location":"api-reference/#PlateMaps.mkedge","page":"API Reference","text":"mkedge(node1, node2, role::Symbol=:default; metadata=Dict{Symbol,Any}())\n\nConstruct an edge in PlateMaps' native, dependency-free scheduling input format: a NamedTuple (node1=.., node2=.., role=.., metadata=..). This shape deliberately mirrors RunMaps.edges(rm)'s (run, linked_run, relation_type, metadata) output (with neutral field names, since PlateMaps doesn't know about a run/control distinction) – a RunMap's edges can be handed to the scheduler with only a field-name translation, no real conversion logic.\n\nAny iterable of NamedTuples with node1/node2/role/metadata fields is a valid edges argument to schedule_platemap; this constructor is just a convenience for hand-building them.\n\n\n\n\n\n","title":"PlateMaps.mkedge"},{"category":"method","location":"api-reference/#PlateMaps.node_roles-Tuple{Any}","page":"API Reference","text":"node_roles(edges) -> Dict{Any,Set{Symbol}}\n\nThe set of roles each node participates in, derived from edges (a node's roles are the union of .role over every edge touching it, from either side – PlateMaps doesn't track a run/control-style asymmetry between an edge's two endpoints).\n\n\n\n\n\n","title":"PlateMaps.node_roles"},{"category":"method","location":"api-reference/#PlateMaps.nodes-Tuple{PlateMap}","page":"API Reference","text":"nodes(pm::PlateMap{T}) where T -> Vector{T}\n\nAll nodes currently placed on pm, in no particular order.\n\n\n\n\n\n","title":"PlateMaps.nodes"},{"category":"method","location":"api-reference/#PlateMaps.place_runs-Tuple{BitMatrix, Any, Any}","page":"API Reference","text":"place_runs(wells::BitMatrix, run_nodes, edges; restarts::Int=10, iterations::Int=1000,\n           rng::AbstractRNG=Random.default_rng()) -> Dict{Any,CartesianIndex{2}}\n\nFix run-node positions before control placement, minimizing run_compactness – edge-connected groups of runs are clustered together (via the same restart/local-move search schedule_platemap uses for control placement) so that a run's specific linked control isn't left stranded far from it. Runs with no edges, or whose whole component has no other run member, are unconstrained by the objective and land wherever the initial random draw puts them.\n\nKeyword Arguments\n\nrestarts/iterations: search effort, independent of the control solver's own restarts/iterations.\nrng: random number generator used for the initial placement and every search move.\n\n\n\n\n\n","title":"PlateMaps.place_runs"},{"category":"method","location":"api-reference/#PlateMaps.platemap_to_json-Tuple{PlateMap}","page":"API Reference","text":"platemap_to_json(pm::PlateMap) -> String\n\nSerialize a PlateMap to a JSON string.\n\nSee also: json_to_platemap\n\n\n\n\n\n","title":"PlateMaps.platemap_to_json"},{"category":"method","location":"api-reference/#PlateMaps.platemaps_from_dataframe-Tuple{DataFrames.DataFrame}","page":"API Reference","text":"platemaps_from_dataframe(df::DataFrame) -> Vector{PlateMap}\n\nReverse of DataFrame(pms::Vector{PlateMap}): splits df on its required plate column (sorted ascending) and reconstructs each plate via the existing PlateMap(df) on that group's well,row,col,active,occupant columns.\n\n\n\n\n\n","title":"PlateMaps.platemaps_from_dataframe"},{"category":"method","location":"api-reference/#PlateMaps.platemaps_to_json-Tuple{Vector{PlateMap}}","page":"API Reference","text":"platemaps_to_json(pms::Vector{PlateMap}) -> String\n\nSerialize a multi-plate schedule to a single JSON string: a __type__-tagged \"PlateMapBatch\" wrapper around each plate's own JSON.lower form, matching the tagged-Dict convention every other JSON shape in this package uses. Deliberately not a JSON.lower(::Vector{PlateMap}) method, so an unrelated Vector{PlateMap} nested inside some other JSON structure isn't silently reshaped by it.\n\nSee also: json_to_platemaps\n\n\n\n\n\n","title":"PlateMaps.platemaps_to_json"},{"category":"method","location":"api-reference/#PlateMaps.role_neighbors-Tuple{Any}","page":"API Reference","text":"role_neighbors(edges) -> Dict{Any,Dict{Symbol,Set{Any}}}\n\nFor every node, the set of other nodes it shares a same-role edge with, grouped by role – the adjacency structure total_distance needs to find each node's nearest same-role neighbor.\n\n\n\n\n\n","title":"PlateMaps.role_neighbors"},{"category":"method","location":"api-reference/#PlateMaps.roles-Tuple{Any}","page":"API Reference","text":"roles(edges) -> Set{Symbol}\n\nThe distinct roles referenced by edges.\n\n\n\n\n\n","title":"PlateMaps.roles"},{"category":"method","location":"api-reference/#PlateMaps.run_compactness-Tuple{Any, Any, Any}","page":"API Reference","text":"run_compactness(pos, edges, run_nodes) -> Real\n\nFor every edge-connected component (see components) with 2 or more run_nodes members, sum each member's Manhattan distance to the component's centroid (mean row, mean col of its members' pos). Components with fewer than 2 run-node members (a run with no edges, or whose whole component has no other run) contribute 0 – there is nothing to be compact relative to. Used by place_runs to cluster edge-linked runs together before control placement, so a run's specific control isn't left stranded far from it.\n\n\n\n\n\n","title":"PlateMaps.run_compactness"},{"category":"method","location":"api-reference/#PlateMaps.scale-Tuple{Real, Real, Real}","page":"API Reference","text":"scale(x,lb,ub) -> Real\n\nNormalize x into [0,1] given known bounds lb/ub (matches PlateArrays' identically-named helper).\n\n\n\n\n\n","title":"PlateMaps.scale"},{"category":"method","location":"api-reference/#PlateMaps.schedule_platemap-Tuple{BitMatrix, Any, Any}","page":"API Reference","text":"schedule_platemap(wells::BitMatrix, edges, fixed_nodes; plate_solver::String=\"greedy\",\n                   solver::String=\"exchange\", run_restarts::Int=10, run_iterations::Int=1000,\n                   rng::AbstractRNG=Random.default_rng(), kwargs...) -> Vector{PlateMap}\n\nSchedule edges/fixed_nodes across one or more identically-shaped wells plates. Edge-connected components (see components) are atomic – each one lands wholly on a single plate, never split – so the first stage is a bin-packing problem (which components share which plate, minimizing plate count), and the second stage is _schedule_single_plate's existing two-stage placement, run once per plate on that plate's subset of edges/fixed_nodes.\n\nReturns one PlateMap per plate actually used (never zero – an entirely empty edges/fixed_nodes input still returns a 1-element vector holding one empty PlateMap).\n\nArguments\n\nwells: active-well mask, shared by every plate (this package doesn't support heterogeneous plate shapes in one call).\nedges: an iterable of (node1,node2,role,metadata)-shaped NamedTuples (see mkedge).\nfixed_nodes: the nodes to place first on their plate, before any optimization (typically \"runs\").\n\nKeyword Arguments\n\nplate_solver: \"greedy\" (default, first-fit-decreasing, exact for uniform component sizes) or \"MILP\" (exact minimum plate count) – see plate_solvers/_plate_assign_greedy/ _plate_assign_MILP. Throws ArgumentError if any single component is larger than one plate's active-well count.\nsolver: \"exchange\" (default, heuristic, supports every objective) or \"MILP\" (exact, objective=:distance only – see _milp_solve's docstring for why, and its bipartite requirement on edges).\nrun_restarts/run_iterations/rng: passed to place_runs to fix fixed_nodes' positions on each plate (kept separate from the control solver's own restarts/iterations below).\nother keywords (objective, minimize, restarts, iterations, lambda, timelimit, optimizer, ...) are forwarded to the chosen solver (and, for plate_solver=\"MILP\", to _plate_assign_MILP too – optimizer/timelimit are shared across both MILP stages when both are selected).\n\n\n\n\n\n","title":"PlateMaps.schedule_platemap"},{"category":"method","location":"api-reference/#PlateMaps.total_distance-Tuple{Any, Any}","page":"API Reference","text":"total_distance(pos, edges) -> Real\n\nFor every node, and every role it participates in, the minimum distance to any same-role neighbor, summed over every (node, role) pair – the core generalized \"minimax\"-style objective. This is not a sum over every edge: a node linked to several same-role neighbors is only scored by its distance to the closest one, matching PlateArrays' old distance_score_brute (which scored every active well's distance to its nearest positive/negative control) generalized to however many roles are present, rather than penalizing a node for being far from every same-role neighbor it has.\n\n\n\n\n\n","title":"PlateMaps.total_distance"},{"category":"method","location":"api-reference/#PlateMaps.total_evenness-Tuple{BitMatrix, Any, Any}","page":"API Reference","text":"total_evenness(wells::BitMatrix,pos,edges) -> Real\n\nevenness summed over every distinct role present in edges.\n\n\n\n\n\n","title":"PlateMaps.total_evenness"},{"category":"method","location":"api-reference/#PlateMaps.well_position-Tuple{PlateMap, Any}","page":"API Reference","text":"well_position(pm::PlateMap, node) -> Union{CartesianIndex,Nothing}\n\nThe well node occupies, or nothing if it isn't placed on pm. A linear scan (O(wells)) – plate sizes here are small enough (hundreds of wells) that a cached reverse index isn't worth the complexity.\n\n\n\n\n\n","title":"PlateMaps.well_position"},{"category":"function","location":"api-reference/#PlateMaps.wells_from_locationkind","page":"API Reference","text":"wells_from_locationkind(kind)\n\nDerive an active-well BitMatrix from a registered plate kind's shape. Provided by the CHESSCore package extension (using CHESSCore activates it).\n\n\n\n\n\n","title":"PlateMaps.wells_from_locationkind"},{"category":"method","location":"api-reference/#RecipesBase.plot-Tuple{PlateMap}","page":"API Reference","text":"plot(pm::PlateMap; inactive=\"darkgray\", empty_color=\"white\", occupied_color=\"steelblue\")\n\nBasic layout visualization: inactive wells, empty active wells, and occupied wells in three flat colors, labeled by raw node identity where displayable. This is the dependency-free view – it has no notion of what an occupant is (role, relationships), since PlateMap doesn't store that. For a role-aware, colored-by-role plot, see the RunMaps extension's plot(pm, rm).\n\n\n\n\n\n","title":"RecipesBase.plot"},{"category":"section","location":"#PlateMaps.jl","page":"Home","text":"PlateMaps is the physical-placement half of plate scheduling in CHESS: given a set of nodes and the edges that relate them, it decides where on a plate (or plates) each node goes.\n\nThe relationship half – deciding what is connected to what (which run needs which controls, which runs are duplicates of each other, and so on) – is deliberately a separate concern, owned by the sibling package RunMaps. PlateMaps has no built-in notion of \"run\" or \"control\": its core type, PlateMap, just tracks which node occupies which well.","title":"PlateMaps.jl"},{"category":"section","location":"#Core-type","page":"Home","text":"struct PlateMap{T}\n    wells::BitMatrix                     # which grid cells are usable\n    occupant::Matrix{Union{Missing,T}}   # which node sits in each well\nend\n\nT is the caller-chosen node-identity type (an Int, a Symbol, a RunMap node id – whatever the edges use). PlateMap is the solution to a scheduling problem, not the problem statement.","title":"Core type"},{"category":"section","location":"#Three-ways-to-use-it","page":"Home","text":"Standalone, dependency-free. Build your own edges with mkedge and call schedule_platemap directly – no other CHESS package required.\nWith RunMaps. A weak-dependency extension (loaded automatically when both packages are using'd) adds schedule_platemap(wells, rm::RunMap, placeable_roles; kwargs...), plus RunMap-aware describe, plot, and DataFrame methods.\nWith CHESSCore. Another weak-dependency extension adds wells_from_locationkind and schedule_platemap(kind::CHESSCore.LocationKind, ...), so a registered plate LocationKind can stand in for a hand-built wells::BitMatrix – and composes with the RunMaps extension automatically (schedule_platemap(kind, rm, placeable_roles; kwargs...) works with both loaded).\n\nExtension docstrings (PlateMapsRunMapsExt, PlateMapsCHESSCoreExt) aren't pulled into the API Reference page's @autodocs block, since Documenter doesn't load weak-dependency extension modules the way it loads PlateMaps itself – see the Quick Start Guide for their usage instead.\n\nSee the Quick Start Guide for a full walkthrough, including multi-plate scheduling and the DataFrame/JSON interfaces.","title":"Three ways to use it"}]
}
